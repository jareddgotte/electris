# v0.2.0-rc.2

## Status

This file is a **documentation-only readiness artifact**. It records the
next approved channel named in `RELEASING.md` and #55, proposed after the
failed and unpublished `v0.2.0-rc.1` canary. As of this exact-head repository
state:

- No `v0.2.0-rc.2` tag, workflow run, draft, GitHub Release, or public asset
  set exists.
- The immutable `v0.2.0-rc.1` tag remains failed, unpublished canary
  evidence: its authorized exact-tag target-failure stage ran, Windows
  independently exposed the now-fixed PowerShell path-expansion defect in the
  tagged prepare workflow before package smoke or archival, assembly was
  skipped, both temporary canary variables were removed, and no rc.1 Release
  or public asset set was ever produced. See "Canary and recovery proof" in
  [`../release-administration.md`](../release-administration.md) and
  "Failure recovery and rollback" in `RELEASING.md` for the exact disposition.
  The `v0.2.0-rc.1` tag is never moved, deleted, or reused; this proposal is
  the corrected `rc.2` channel that rule anticipates.
- The PowerShell defect that caused the rc.1 Windows canary failure is landed
  on protected `master` via merged PR #54.
- Committing this file does not authorize creating the tag or running either
  release workflow. Tag creation and publication each require separate
  captain authorization, per `RELEASING.md`.
- The hard-coded exact-tag canary hooks and their tests remain targeted at
  `v0.2.0-rc.1` only. Retargeting them to `v0.2.0-rc.2` is a later, separately
  authorized, issue-linked pull request and is not part of this proposal.

## Scope of this proposal

This version bump and note are prepared under #55, which scopes only the
version/release-note proposal itself. Preparation (tagging, native packaging,
draft assembly) and publication remain separate, later, separately authorized
steps.

## Readiness evidence and remaining gates

The following verified or documented facts now apply to the rc.2 stream, all
carried over unchanged from the rc.1 stream because no gate resolution work is
in scope for this proposal:

- Dependency remediation has been landed in the repository; the earlier
  audit-finding state is not the documented baseline.
- The native-runner qualification workflow is merged and available for a
  separately authorized protected-`master` dispatch.
- The approved release-administration controls are documented in
  `docs/release-administration.md` and remain separate from any release action.

The following boundaries still apply and must be preserved before any public
release:

- GitHub private vulnerability reporting must be enabled and verified.
- Signing/notarization credentials and the protected `release-signing`
  environment are not part of this documentation step; macOS artifacts remain
  qualification-only until separately approved.
- Matching native runner labels/capacity for the full release matrix (Linux
  x64, Windows x64, macOS arm64, macOS x64) must be verified by the approved
  runner-qualification workflow before any release claim.
- Administrator rollout must stay aligned with `docs/release-administration.md`;
  the separately undecided default-branch protection ruleset is not changed by
  this document.
- The hard-coded exact-tag canary hooks and their tests are unchanged and
  remain targeted at `v0.2.0-rc.1` only; a separately authorized, issue-linked
  pull request retargets them before an rc.2 canary can run.

## Planned assets (never built)

`v0.2.0-rc.2` has no tag, workflow run, or preparation attempt, so the assets
below, planned per `RELEASING.md`, do not yet exist:

- `electris-v0.2.0-rc.2-linux-x64-portable.tar.gz` (unsigned)
- `electris-v0.2.0-rc.2-win32-x64-portable.zip` (unsigned)
- `electris-v0.2.0-rc.2-release-manifest.json`
- `electris-v0.2.0-rc.2-SHA256SUMS.txt`
- Qualification-only `electris-v0.2.0-rc.2-darwin-arm64.zip` and
  `electris-v0.2.0-rc.2-darwin-x64.zip`, excluded from the public GitHub
  Release asset set while unsigned.

## Support and security status

Electris has no current supported release. This prerelease, once published,
would be an unsupported evaluation candidate with best-effort assistance and
no response-time SLA, consistent with `SUPPORT.md`. `SECURITY.md`'s
disposition guidance applies unchanged.

## Migration and rollback

No user-facing gameplay or data-format changes are proposed by this version
bump. Existing artwork and the Electris name are retained unchanged, with no
broader legal claim asserted. If this channel also fails to publish,
correction is a further new SemVer/tag via a corrected pull request, per
"Failure recovery and rollback" in `RELEASING.md`.
