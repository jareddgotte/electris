# v0.2.0-rc.1

## Status

This file is a **documentation-only readiness artifact**. It records the
next approved channel named in `RELEASING.md` and #38. As of this exact-head
repository state:

- No `v0.2.0-rc.1` tag exists.
- No release-prepare or release-publish workflow has run for this version.
- No archive, checksum, manifest, or GitHub Release has been built, signed,
  notarized, or published for this version.
- The dependency remediation, native-runner qualification workflow, exact-tag
  canary/recovery hooks, and release-administration controls are present in the
  repository, but they remain documentation and workflow evidence only.
- Committing this file does not authorize creating the tag or running either
  release workflow. Tag creation and publication each require separate
  captain authorization, per #38's ordered rollout and `RELEASING.md`.

## Scope of this proposal

This version bump and note are prepared under #41, which scopes only the
version/release-note proposal itself. Preparation (tagging, native packaging,
draft assembly) and publication remain separate, later, separately authorized
steps.

The repository now also contains the reviewed release-preparation workflow,
pre-tag runner qualification workflow, and canary/recovery hooks needed for the
authorized rc.1 stream. Those changes do not mean any of the associated actions
have occurred.

## Readiness evidence and remaining gates

The following verified or documented facts now apply to the rc.1 stream:

- Dependency remediation has been landed in the repository; the earlier
  audit-finding state is no longer the documented baseline.
- The native-runner qualification workflow is merged and available for a
  separately authorized protected-`master` dispatch.
- The deterministic rc.1 canary/recovery hooks are present for the guarded
  preparation workflow.
- The approved release-administration controls are documented in
  `docs/release-administration.md` and remain separate from any release action.

The following boundaries still apply and must be preserved before any public
release:

- GitHub private vulnerability reporting must be enabled and verified.
- Signing/notarization credentials and the protected `release-signing`
  environment are not part of this documentation step; macOS artifacts remain
  qualification-only until separately approved.
- Matching native runner labels/capacity for the full release matrix (Linux
  x64, Windows x64, macOS arm64, macOS x64) must be verified by the approved
  runner-qualification workflow before any release claim.
- Administrator rollout must stay aligned with `docs/release-administration.md`;
  the separately undecided default-branch protection ruleset is not changed by
  this document.

## Planned assets (not yet built)

When preparation is separately authorized and the `v0.2.0-rc.1` tag is
created, `release-prepare.yml` is expected to produce, per `RELEASING.md`:

- `electris-v0.2.0-rc.1-linux-x64-portable.tar.gz` (unsigned)
- `electris-v0.2.0-rc.1-win32-x64-portable.zip` (unsigned)
- `electris-v0.2.0-rc.1-release-manifest.json`
- `electris-v0.2.0-rc.1-SHA256SUMS.txt`
- Qualification-only `electris-v0.2.0-rc.1-darwin-arm64.zip` and
  `electris-v0.2.0-rc.1-darwin-x64.zip`, excluded from the public GitHub
  Release asset set while unsigned.

## Support and security status

Electris has no current supported release. This prerelease, once published,
would be an unsupported evaluation candidate with best-effort assistance and
no response-time SLA, consistent with `SUPPORT.md`. `SECURITY.md`'s disposition
guidance applies unchanged.

## Migration and rollback

No user-facing gameplay or data-format changes are proposed by this version
bump. Before any tag exists, rollback is a normal revert of the version-bump
commit. After a future tag or publication, correction requires a new SemVer
version; existing tags and published bytes are never moved or replaced.
